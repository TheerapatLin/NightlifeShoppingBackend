const express = require('express');
const router = express.Router();

// Placeholder for business routes
router.get('/', (req, res) => {
  res.json({ message: 'Business routes - placeholder' });
});

module.exports = router;
