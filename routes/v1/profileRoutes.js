const express = require('express');
const router = express.Router();

// Placeholder for profile routes
router.get('/', (req, res) => {
  res.json({ message: 'Profile routes - placeholder' });
});

module.exports = router;

