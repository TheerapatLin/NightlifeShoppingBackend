const express = require('express');
const router = express.Router();

// Placeholder for refund routes
router.get('/', (req, res) => {
  res.json({ message: 'Refund routes - placeholder' });
});

module.exports = router;
