const express = require('express');
const router = express.Router();

// Placeholder for affiliate routes
router.get('/', (req, res) => {
  res.json({ message: 'Affiliate routes - placeholder' });
});

module.exports = router;

