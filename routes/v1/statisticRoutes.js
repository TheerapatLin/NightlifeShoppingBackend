const express = require('express');
const router = express.Router();

// Placeholder for statistic routes
router.get('/', (req, res) => {
  res.json({ message: 'Statistic routes - placeholder' });
});

module.exports = router;

